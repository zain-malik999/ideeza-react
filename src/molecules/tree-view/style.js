import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles({
  root: {
    width: "100%",
    display: "flex",
    flexDirection: "column",
    height: "80vh",
    overflow: "auto",
  },
});
export { useStyles };
